# TEST-PLAN — keyboard_adapter

Спочатку сценарії та граничні значення, потім код тестів (правило з CLAUDE.md).
Усе тестується на чистій логіці (`axes.py`) і фейкових залежностях — без реальної
клавіатури, без мережі, без `sleep` на реальному часі.

## Модель, яку перевіряємо

- Осі `roll`/`pitch`/`yaw`: `value += k*dt` поки натиснута клавіша напрямку;
  коли не натиснута — рух до нуля з тим самим `k`, **без проскакування нуля**.
- `throttle`: `value += k*dt` поки натиснута; коли відпущена — **утримує** значення.
- Кліп у `[-1000, 1000]`.
- Відправка `MANUAL_CONTROL` — фіксовані 20 Гц, незалежно від подій клавіатури.

Константи: `RANGE = 1000`, `RATE = k` (одиниць/с, дефолт 2000 ⇒ 0..1000 за 0.5 с).

## Сценарії — пружинні осі (roll/pitch/yaw)

| # | Сценарій | Очікування |
|---|---|---|
| 1 | нуль, жодної клавіші | лишається рівно `0.0`, не йде в мінус |
| 2 | утримання рівно до досягнення межі | після `RANGE/RATE` с значення рівно `1000` |
| 3 | утримання понад межу | кліп `1000`, без переповнення (те саме і для `-1000`) |
| 4 | відпускання з максимуму, покроково | `1000 → 1000-k*dt → ...`; за `RANGE/RATE` с рівно `0` |
| 5 | spring-return з великим `dt` (перестрибування через нуль) | рівно `0.0`, не від'ємне |
| 6 | spring-return з від'ємного максимуму | рух вгору, зупинка рівно на `0.0` |
| 7 | A+D (протилежні клавіші одночасно) | нетто-вхід 0 ⇒ поводиться як відпущена (spring-return) |
| 8 | A+D при нулі | лишається `0.0` |
| 9 | `dt = 0` | значення не змінюється (ні під натиском, ні у spring-return) |
| 10 | `dt < 0` | ігнорується, значення не змінюється (захист від стрибка годинника назад) |
| 11 | аномально великий `dt` (напр. 10 с) під натиском | кліп у `1000`, не NaN/не переповнення |
| 12 | зміна напрямку без відпускання (D → A з `+1000`) | значення зменшується від `+1000` і **може** перетнути нуль (активний вхід ≠ spring-return) |
| 13 | послідовність натиск-відпуск-натиск | значення відновлює наростання з поточного, не з нуля |

## Сценарії — throttle

| # | Сценарій | Очікування |
|---|---|---|
| 14 | Shift (up) утримується | наростання з тим самим `k` |
| 15 | Ctrl (down) утримується | спадання |
| 16 | відпускання після набору | значення **не змінюється** (немає spring-return) |
| 17 | кліп throttle | `[-1000, 1000]` так само |
| 18 | Shift+Ctrl одночасно | нетто 0 ⇒ утримання (не повернення до нуля) |

## Розкладка

| # | Сценарій | Очікування |
|---|---|---|
| 19 | `W`/`S` → pitch, `A`/`D` → roll, `Q`/`E` → yaw, `shift`/`ctrl` → throttle | натиск однієї клавіші рухає рівно одну вісь, решта лишаються `0` |
| 20 | невідома клавіша (`z`) | нічого не рухається |
| 21 | регістр клавіші (`W` vs `w`) | однаковий ефект |

## Сценарії — відправка (loop)

| # | Сценарій | Очікування |
|---|---|---|
| 22 | 1 с при 20 Гц з керованим годинником | рівно 20 викликів `mav.manual_control_send` |
| 23 | 0.5 с при 20 Гц | рівно 10 викликів |
| 24 | жодної клавіші не натиснуто за весь інтервал | пакети все одно йдуть (відправка не подієва) |
| 25 | вміст пакета | осі передані як `int`, у порядку `x=pitch, y=roll, z=throttle, r=yaw`, `buttons=0` |
| 26 | `z` (throttle) у пакеті | MANUAL_CONTROL z — той самий `[-1000, 1000]`, як і решта осей |

Фейки: `FakeMav` (записує виклики), `FakeClock` (монотонний лічильник + `sleep`, що
просто рухає час) — жодного реального часу.

## Сценарії — `evdev_source.py` (Wayland-бекенд)

`evdev` — лінукс-специфічний пакет (немає на macOS/CI), тому в `sys.modules["evdev"]`
підміняється фейковий модуль (`tests/evdev_fakes.py`) ще до виклику коду з локальним
`import evdev`. Реальний пристрій і реальний потік ніде не потрібні: `_run()`/`feed()`
викликаються напряму, синхронно, на скінченних списках подій.

### `normalize_evdev` (чиста функція)

| # | Сценарій | Очікування |
|---|---|---|
| E1 | `KEY_W` (17) | `"w"` |
| E2 | `KEY_A` (30) | `"a"` |
| E3 | `KEY_LEFTSHIFT` (42) і `KEY_RIGHTSHIFT` (54) | обидва → `"shift"` |
| E4 | `KEY_LEFTCTRL` (29) і `KEY_RIGHTCTRL` (97) | обидва → `"ctrl"` |
| E5 | код поза `KEYCODE_TO_NAME` (напр. `KEY_ESC`=1) | `None` |

### `EvdevSource.feed` (стан натиснутих клавіш)

| # | Сценарій | Очікування |
|---|---|---|
| E6 | `feed(17, KEY_DOWN)` | `"w"` у `snapshot()` |
| E7 | `feed(17, KEY_DOWN)` потім `feed(17, KEY_UP)` | `snapshot()` порожній |
| E8 | `feed(17, KEY_REPEAT)` без попереднього `KEY_DOWN` | `snapshot()` порожній (repeat не додає) |
| E9 | `feed(17, KEY_DOWN)` потім `feed(17, KEY_REPEAT)` | `"w"` лишається (repeat не знімає) |
| E10 | `feed(<невідомий код>, KEY_DOWN)` | `snapshot()` порожній |

### `EvdevSource._run` (диспетчеризація подій пристрою)

| # | Сценарій | Очікування |
|---|---|---|
| E11 | скінченний потік `[W down, S down, S up]` | фінальний `snapshot() == {"w"}` |
| E12 | подія з `type != EV_KEY` (напр. `EV_SYN`=0) серед потоку | ігнорується, не валить обробку решти |

### `EvdevSource.stop`

| # | Сценарій | Очікування |
|---|---|---|
| E13 | виклик `stop()` | базовий `device.close()` викликано, `_stop_event` встановлено |

### `find_keyboard_device` (автовизначення)

| # | Сценарій | Очікування |
|---|---|---|
| E14 | серед пристроїв є один з `KEY_A` у `capabilities()[EV_KEY]` | повертає саме його шлях |
| E15 | жоден пристрій не має `KEY_A` | `RuntimeError` зі згадкою групи `input` |
| E16 | кілька пристроїв мають `KEY_A` | повертає ПЕРШИЙ за порядком `list_devices()` |

### `EvdevSource.start` (вибір шляху пристрою)

| # | Сценарій | Очікування |
|---|---|---|
| E17 | `device_path` заданий явно | `find_keyboard_device`/`list_devices()` НЕ викликається (фейк кидає `AssertionError`, якщо викликати) |
| E18 | `device_path=None` | автовизначення відпрацьовує, події з знайденого пристрою обробляються |

### Відоме обмеження (задокументоване, не виправляється)

Обидва фізичні Shift (і обидва Ctrl) мапляться на одне логічне ім'я — одночасне
натискання Left+Right із відпусканням лише одного передчасно зніме стан. Для
клавіатурного керування літаком не є реалістичним сценарієм польоту, тест не пишеться.

### `EvdevSource._run` — `OSError` під час `stop()` (виявлено живим дебагом на VM)

`stop()` закриває пристрій конкурентно з блокуючим читанням у фоновому потоці —
ядро повертає `OSError`/`EBADF`, необроблений, всередині потоку. Очікувано під
час зупинки, шумно й неправильно як необроблений виняток інакше.

| # | Сценарій | Очікування |
|---|---|---|
| E19 | `read_loop()` кидає `OSError` **після** `stop()` (`_stop_event` вже встановлено) | глушиться, `_run` завершується без винятку |
| E20 | `read_loop()` кидає `OSError` **без** попереднього `stop()` | пробрасується — це не race, а реальна відмова пристрою |

### `cli.py` — `--dry` і `--debug` (додано для живої діагностики "чи працює кеймапінг" на VM без SITL)

| # | Сценарій | Очікування |
|---|---|---|
| C1 | `parse_args([])` | `args.dry is False`, `args.debug is False` |
| C2 | `parse_args(["--dry"])` | `args.dry is True` |
| C3 | `NullManualControlSender.manual_control_send(...)` | не кидає винятку (весь сенс класу — no-op для `--dry`) |

### `cli.TickPrinter` — притримуваний живий вивід (перша версія без throttle "спамила" — `\r` не перезаписує рядок на всіх терміналах)

| # | Сценарій | Очікування |
|---|---|---|
| C4 | перший виклик | друкує одразу |
| C5 | другий виклик у межах `min_interval_s` від першого | не друкує (порожній stdout) |
| C6 | другий виклик після `min_interval_s` | друкує |

Регресія, знайдена цими тестами (C4/C6): `sorted(keys) or '-'` при непорожньому
`keys` повертає список — форматування `:<24` на списку кидає `TypeError`. На
практиці це ніколи не спрацьовувало видимо: коли `keys` порожній — все ок,
показує `keys=-`; щойно `evdev` бачив реальну клавішу — впало б з traceback
замість показати результат. Пофіксовано (`" ".join(sorted(keys)) or "-"`) до
того, як це вдарило по живому дебагу на VM.

### `cli.wait_for_vehicle_heartbeat` — реальний бага, знайдений живим польотом на VM

Перший справжній тестовий політ (`FBWA`, повний діапазон стіків через
`keyboard-adapter --debug`) дав усі метрики нульовими вдруге, попри видимий
рух стіків. `RCIN` у `.BIN` лишався на константному нейтралі (`1500/1500/
1000/1500`) весь політ, хоча в `mav.tlog` видно `MANUAL_CONTROL` із повним
діапазоном `x`/`y`/`z`/`r` — пакети долітали до SITL, але ArduPlane їх мовчки
відкидав.

Причина: `pymavlink.wait_heartbeat()` матчить БУДЬ-ЯКИЙ `HEARTBEAT`, включно з
тим, що шле сам GCS (MAVProxy, `sysid=255`), не лише апарата (`sysid=1`).
`conn.target_system` — властивість над `sysid`, яка оновлюється лише для
heartbeat-ів, що проходять фільтр "це апарат" (`probably_vehicle_heartbeat` у
pymavlink). Якщо перший зловлений heartbeat був від GCS — `target_system`
лишається `0`, і оскільки цикл після цього більше нічого не читає з мережі
(тільки шле), лишається `0` назавжди. ArduPlane (`handle_manual_control`,
`GCS_MAVLink_Plane`) окремо звіряє `packet.target == sysid_this_mav()` і
мовчки відкидає все з `target=0` — офіційний autotest ArduPilot для
`MANUAL_CONTROL`/`FBWA` явно шле `target=1`, не `0` (перевірено через
DeepWiki по `ArduPilot/ardupilot`).

| # | Сценарій | Очікування |
|---|---|---|
| C7 | послідовні `recv_match` дають `target_system` `0, 0, 1` (GCS, GCS, апарат) | функція повертається, `conn.target_system == 1` |
| C8 | `target_system` вже нульовий, але не `0` при вході (уже валідний) | `recv_match` не викликається жодного разу |
| C9 | `target_system` лишається `0` попри всі спроби, дедлайн минув | `RuntimeError` |

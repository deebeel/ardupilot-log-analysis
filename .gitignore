.idea
*.iml
worktrees
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
data/
.envrc
deploy/compose/.env
dist/

# Офіційний апстрім ArduPilot, клонується tools/prereqs.sh на пінований тег —
# залежність для збірки/запуску, не частина цього репозиторію (як і assets/ нижче).
.sitl/

# Матеріали замовника (тікет, реальні логи) — не можна публікувати в git.
# Прибрано з робочого дерева та переписано з усієї історії (filter-branch, 2026-09-14).
assets/
